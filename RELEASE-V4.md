# Cockpit Tools Isolation V4：Codex 多账号独立网络与代理库存

基于 [jlcodes99/cockpit-tools v1.3.59](https://github.com/jlcodes99/cockpit-tools/tree/v1.3.59) 的社区修改版，面向主要使用“总 API 服务”的 Windows Codex 多账号用户。每个账号从添加/授权到被 API 服务选中后的上游请求使用自己的代理；代理故障时阻断该账号请求，不回退原始网络。本项目与上游及 OpenAI 均无官方关联。

## V4 功能

- **代理库存与线路组**：加密保存线路；添加新账号或编辑旧账号时快速选择。组内健康检测发现主线路失效后尝试备用线路，沿用该账号的本地监听端口。故障窗口内的请求可能失败，需要客户端重试。
- **多协议**：VLESS Reality/Vision、HTTP(S)、SOCKS5 使用 Xray；Hysteria2 与 TUIC 使用独立 sing-box。带 `insecure=1` 的线路会标出 TLS 证书验证关闭的风险。
- **订阅导入**：一次性解析标准 V2Ray Base64 节点列表或 Clash YAML 的受支持节点；不支持的类型计数跳过。订阅链接不持久化、不自动刷新；拉取前明确选择库存代理或同意本机网络直连。
- **请求可观测**：账号卡展示当前线路名、协议简称和最近模型；详细弹窗保留实际选中账号、请求结果、延迟、令牌量、请求 ID、上次检测出口等。最近请求驱动柔和绿色呼吸灯，约 5 分钟连续褪回默认色；红色仅提示真实故障。
- **宽屏布局**：默认五列，可选自动/4/5/6 列与 85–110% 缩放；API 服务首卡只展示最近调用的三个账号。任一卡片的眼睛开关同步隐藏/显示所有账号卡的远端 IP，线路名称不隐藏。
- **默认客户端不多开**：总 API 服务沿用电脑原装 ChatGPT/Codex 客户端的数据目录；逐账号“隔离启动”才创建独立目录。

## 下载与使用

Windows x64 用户下载本 Release 中的安装 EXE ZIP，退出旧隔离版和原版 Cockpit 后解压并运行安装程序，安装到隔离版目录，不要覆盖官方版。安装后先在一个测试账号上验证代理出口和 API 请求链路，再逐步接入其他账号。

开发者使用 GitHub 自动生成的 `Source code (zip)`，或本 Release 附带的同结构纯源码 ZIP；参见 [从源码构建](SOURCE-BUILD.md)。源码不包含本机工具链、编译成品或用户数据。

## 验证与边界

合成数据验证包括：9 项代理/订阅解析测试、10 项隔离契约测试、307 项前端测试、Go 网关测试，以及 Windows 成品自检（双账号隔离、组内故障接管、Hysteria2/TUIC 配置与监听、直连命中数 0）。**未使用或访问任何真实账号、节点或用户提供的订阅服务**；公网节点和服务商特定订阅仍需使用者自行验证。账号池代理不代表桌面客户端所有网络流量均被代理。

## 许可与归属

上游衍生代码保留 [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/legalcode)：署名、非商业使用、相同方式共享。公开源码不等于授予商业使用权；商业用途应联系上游权利人。Xray-core 为 MPL-2.0，sing-box 为 GPLv3-or-later，CLIProxyAPI 保留其自身许可；许可证与对应来源见 [LICENSE](LICENSE)。本社区版不宣称获得上游项目、OpenAI 或代理内核作者的认可。
