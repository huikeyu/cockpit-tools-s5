# Cockpit Tools · Codex 账号隔离 V4

基于 jlcodes99/cockpit-tools v1.3.59。V4 的关键变化：添加账号前必须填写账号代理；浏览器 OAuth WebView、设备授权、refresh_token 交换、账号检查和 API 服务请求都使用同一账号代理。代理不可用时阻止请求，不回退直连。

支持 VLESS TCP/WS、TLS/Reality、Vision、HTTP(S)、SOCKS5；V4 另使用内置 sing-box 支持 Hysteria2 和 TUIC。代理链接保存在本地加密存储；节点密码不会回显。带 `insecure=1` 或 Clash `skip-cert-verify: true` 的线路会关闭 TLS 证书验证，库存和导入结果会提示风险。sing-box 的 GPLv3 文本及对应 v1.14.1 源码归档随安装包提供。

## V4 代理库存与接管组

“添加账号”和已有账号的“代理设置”均可打开代理库存。库存可新增、编辑名称/分组/链接、删除线路；链接加密保存，列表只显示名称、协议和服务器地址。选择其中一条线路时，账号绑定该线路所属的整个组；同组至少两条线路才有备用。库存组成员变更会同步到已绑定账号，并重启相关本地监听，操作时可能短暂中断当前请求；建议先停止 API 服务。旧版直接粘贴的单线路绑定继续可用，不会被自动改写成库存组。

库存窗口可一次性导入标准 V2Ray Base64 节点列表或 Clash YAML 订阅，只接收 HTTPS 地址，单次响应限 2 MiB、最多处理 2000 条；当前可导入 VLESS、Hysteria2、TUIC、HTTP(S)、SOCKS5，其他格式计数跳过，不会生成假线路。订阅链接仅在本次请求中使用，不保存，也不会自动刷新。用户必须选择一条已有库存线路作为拉取代理，或明确勾选允许本机网络直连订阅服务；后者可能把原始出口 IP 暴露给订阅服务。订阅拉取不发送 Codex 账号凭据。未实际访问用户提供的订阅服务，服务商的具体内容和可用性须安装后验证。

组内线路通过各账号专用的本地代理监听转发，后台每 20 秒开始一轮健康检测；探测和逐条切换还需要额外时间，因此不保证固定接管时限。接管保持同一个本地代理端口供 API 服务使用。检测请求通过该代理访问公网 IP 检测站，不包含账号令牌。认证过程中会锁定已选中的单条线路，以保持从登录到授权换取同一网络；若该线路故障，须取消并重新登录，不会在认证中途换 IP。接管不是无损重放：检测前或切换中的请求可能失败，客户端应重试；不同线路可能有不同公网出口。若整组都失败，或库存组被清空，账号请求阻断，不回退全局代理或原始网络。健康探测只能说明检测目标可达，不保证所有上游站点均可达；重要账号应定期手动核对出口和实际请求日志。

账号卡上柔和绿色呼吸光仅由该账号最近 5 分钟完成的本地 API 请求计算，频繁请求更亮，随后随时间连续褪色；旧日志不会让卡片永久着色。红色只用于真实代理阻断/故障。它不是实时抓包，也不代表正在进行中的请求。外层卡片只留库存线路名称（单行）、协议简称、远端主机/IP 和近期请求模型；点击任意卡片的眼睛会同时切换所有账号卡的远端地址显示，隐藏时保留线路名称，端口不在外层显示。本地监听及上次检测的出口 IP 移入“查看链路”，与请求 ID、令牌、耗时、错误等详情一起展示。宽屏默认五列，工具栏可切换自动/4/5/6 列及 85–110% 缩放。系统减少动态效果时关闭呼吸动画。

API 服务按实际选中的账号路由，账号代理优先于 API 服务全局代理：A 只走 A，B 只走 B。若开启跨账号回退，出口会随被选中的账号变化；需要固定出口时请绑定固定账号并关闭跨账号回退。

## 你当前使用的总 API 服务

第一张 API 服务卡片不需要、也无法代表所有账号选择一个共同出口。卡片摘要只保留“账号独立网络”和最近被调用的最多 3 个账号，不再显示本机地址说明或底部统计行。点击账号的“查看链路”可看到完整路径和本地 API 服务最近完成的请求（每 4 秒刷新），包括实际选中账号、模型、耗时和结果。动画不代表逐包抓包或当前仍在进行的请求。

仅启动本工具不会自动接管桌面客户端。点击总 API 服务的“启动 API 服务”后，V4 会按原版流程将本机 API 凭据和配置写入系统默认 Codex Home，并通过 Windows 系统入口启动电脑原来安装的客户端；不为默认实例传入多开的 Electron 用户数据目录，因此原客户端的本地记录和历史仍在同一位置。配置生效后，进入 `localhost` API 服务的模型请求再由账号池分别代理。客户端网页登录、更新、插件和其他非模型连接不经过这个本机模型 API，也不会继承账号代理；不能把账号池代理理解为整台机器或官方客户端的全流量代理。

添加窗口的浏览器授权、设备授权、Token/JSON、API Key 和本地单账号导入均需要填写“添加账号代理”。可以先点“测试代理出口”核对公网 IP。没有代理不会打开授权窗口、请求设备码或执行 refresh_token 交换。OAuth 使用内置 WebView2 的 `--proxy-server` 参数和独立数据目录绑定代理。严格模式下“官方登录”和批量文件导入已禁用，Token/JSON 一次只导入一个账号。

所有 Codex 账号的本版额度查询和 API 服务上游请求都要求独立代理；未绑定的旧账号会报错，绑定后才能使用。默认桌面客户端若绑定单个账号，也必须使用该账号代理；若绑定“API 服务”合成身份，则允许像原版一样启动默认安装的客户端，无需给合成身份绑定节点。此时只有进入本机 API 的模型请求才由账号池独立代理，客户端自身其他连接仍可能使用原始网络。新账号的代理绑定为必需：节点故障或绑定文件损坏会阻止账号请求；已有账号可在卡片中改绑，严格模式不提供解除后直连。若代理供应商自身动态换出口，公网 IP 仍可能变化，请按账号测试并确认服务商设置。

“隔离启动”额外提供独立 CODEX_HOME、Electron user-data、登录态、会话和代理环境。不修改系统代理；不是虚拟机、设备指纹隔离或 Windows Sandbox。外部浏览器、其他程序和已运行客户端不由本程序接管。

V4 沿用 V2 的 Cockpit 账号和本机配置目录，保留已有代理绑定与 API 服务设置。默认桌面客户端则有意使用系统的 `CODEX_HOME`（未设置时为 `~/.codex`）及其原有 Electron 数据；启动总 API 服务会像原版一样更新该默认 profile 的 API 凭据与配置。若仍使用原版 Cockpit 管理同一默认客户端，请先退出另一版，不要同时切换或启动它们；建议先备份重要的默认 Codex 配置。逐账号“隔离启动”继续使用独立目录。修改单号代理前停止总 API 服务，再重启服务，以清除旧连接。

第三方模型供应商的独立“获取模型/用量”管理页可能使用独立上游凭据，当前不纳入 Codex 账号代理保证。请在配置第三方供应商时单独确认其网络出口；本版的严格阻断针对 Codex 账号添加、额度与 API 服务请求。

双击 `build.cmd` 可重新编译。工装执行 TypeScript、Rust、Go 和成品自检，最终只生成一个按 V4/时间戳命名、仅含安装 EXE 的 ZIP；内部临时目录用于自检，不作为第二交付包。运行前关闭 V4 dev 服务，避免 Windows 锁住 esbuild。
