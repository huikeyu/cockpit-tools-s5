# V4 快速运行

1. 解压 `V4-1.3.59-win-x64-时间戳.zip`，里面只有安装 EXE。先退出 V3 和原版 Cockpit，再双击 `Cockpit Tools Isolation V4_1.3.59_x64-setup.exe` 更新隔离版。
2. 打开 Codex 页的“添加账号”。可粘贴 VLESS、Hysteria2、TUIC、HTTP(S)、SOCKS5 链接，也可点“从代理库存选择”，新建或编辑线路并填写相同的“故障接管组”名称。至少为一组保存两条线路，才能在主线路故障时接管。
3. 库存窗口还可一次性导入 V2Ray Base64 或 Clash YAML 订阅。选已有库存线路拉取，或者明确勾选允许本机网络直连订阅服务（可能暴露本机出口 IP）；导入后在组中选择线路。订阅不会自动刷新。
4. 先测试出口 IP，再登录或导入账号。账号卡摘要显示库存线路名称、协议简称及不带端口的远端地址；任一卡片的眼睛开关会同步隐藏/显示所有账号卡的远端 IP，线路名称始终可见。最近请求的模型名称在右下角突出显示；本地监听和上次出口 IP 在“查看链路”中。
5. 现有账号可在“代理设置”里选择库存线路改绑。修改账号代理前先停止总 API 服务；编辑库存会短暂重启使用该组的本地代理监听，建议在服务停止时操作。
6. 第一张 API 服务卡片最多显示最近调用的 3 个账号；工具栏可选自动/4/5/6 列和 85–110% 缩放。点击“启动 API 服务”会按原版流程更新系统默认 Codex Home 并启动电脑原来安装的客户端，不创建新的 Electron 用户数据目录；“API 服务”合成身份不需要另绑账号代理。只有进入本机 API 的模型请求才按选中账号使用各自节点，客户端其他连接不在这项保证内。使用期间保持本工具后台运行。

V4 接续 V2 的 Cockpit 数据目录：`%LOCALAPPDATA%\CockpitTools-Isolation\V2`，保留账号、代理绑定及 API 服务配置；默认客户端则使用系统原有的 `CODEX_HOME`（未设置时为 `~/.codex`）。请先退出 V2 和原版 Cockpit，再用 V4 切换默认客户端；不要同时运行多个 Cockpit 管理同一个默认客户端。ChatGPT/Codex 客户端的登录、更新等非模型流量不由 API 服务账号池代理，详情见 `V4-ISOLATION-README.md`。
